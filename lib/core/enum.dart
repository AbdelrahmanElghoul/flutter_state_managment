enum RequestType { GET, PUT, DELETE, POST }
enum PaymentMethod { CASH, CARD }
enum AudioPlayerState { PLAYING, PAUSED, IDLE }
// enum OrderState { Placed, Pick_Up, In_process, Delivered }
// enum MapUsage { SELECT_FROM_REGION, SELECT_LOCATION }
// enum Gender { MALE, FEMALE, OTHER }
